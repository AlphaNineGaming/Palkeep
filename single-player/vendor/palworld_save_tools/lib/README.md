# Generated native compression module

The Windows `ooz.pyd` module is compiled from the pinned `pyooz` and `ooz`
source commits in `dependencies.lock.json`. Linux binaries are neither needed
nor shipped by this Windows application.

Run `npm run prepare:single-player` to create `windows/ooz.pyd` locally.
